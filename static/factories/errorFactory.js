/**
 * Factory: errorFactory
 */

app.factory('errorFactory', ["$q", "$http", "$location", "$translate", "dataFactory", function ($q, $http, $location, $translate, dataFactory) {
    'use strict';

    var exports = {};

    return exports;
}]);