/**
 * Factory: notificationFactory
 */

app.factory('notificationFactory', ["$q", "$http", "$location", "$translate", "dataFactory", "errorFactory", function ($q, $http, $location, $translate, dataFactory, errorFactory) {
    'use strict';

    var exports = {};

    

    return exports;
}]);